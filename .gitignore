node_modules
.next
dist
out
build
.open-next

.env
.env.*
!.env.example
!.env.local.example

*.log
*.raw-llm-stream.json
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel
coverage

# Blitzy agent working artifacts (screenshots, screen recordings, scratch output).
# These are local validation/debugging outputs, never product deliverables, so
# they must never be committed to the repository.
blitzy/
